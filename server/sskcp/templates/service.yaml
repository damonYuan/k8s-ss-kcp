apiVersion: v1
kind: Service
metadata:
  name: {{ include "sskcp.fullname" . }}
  labels:
    {{- include "sskcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sskcp.selectorLabels" . | nindent 4 }}
